<%= button_to "Start Task", start_task_path(task), class: "btn btn-default", method: :patch %>
<%= button_to "Remove Task", task_path(task), class: "btn btn-default", method: :delete, data: { confirm: "Are you sure?" } %>

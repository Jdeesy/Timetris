<%= form_for task do |f| -%>
  <% f.label :due_date %>
  <%= f.datetime_local_field :due_date, ampm: true, :order => [:month, :day, :year, :hour, :minute] %>
  <%= f.submit title: "Update Due Date"%>
<% end %>

